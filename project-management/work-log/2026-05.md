# Work log — 2026-05

Append new lines at the **bottom**. Link commits or PRs when useful.

---

2026-05-02 — **WX-002** — 1Password CLI integrated (`eval "$(op signin)"`); vaults **Private** / **Shared**; OpenAI API credential and device/admin secrets sourced from vault into gitignored [`lexie-server/.env`](../../lexie-server/.env). Registry: **WX-002** `done`.

2026-05-02 — **WX-017** — Real **`POST /explain`** path verified: **`OPENAI_API_KEY`** set; **ffmpeg** / **ffprobe** on **`PATH`**; **`pytest -v`:** **8 passed**; live smoke **`POST /explain`** (1s WAV + device Bearer) → **200** `audio/mpeg`. Registry: **WX-017** `done`, **WX-018** `in_progress`.

2026-05-03 — **WX-018** — M0 **C6** closed: code path + README + tests confirm no raw upload audio persisted as files under **`LEXIE_DATA_DIR`** on default **`POST /explain`**; **`LEXIE_LOG_REQUESTS=0`** default and DB logging only when enabled. **`pytest`:** **15 passed**. Registry: **WX-018** `done`; **M0 Part C** (**WX-014**–**WX-018**) complete. Next: **WX-006** (deploy) or **WX-007** / **WX-008** per inventory.

2026-05-03 — **WX-006** — Started M1 deploy track: added [`lexie-server/Dockerfile`](../../lexie-server/Dockerfile) (Python 3.11 + **ffmpeg**, **`LEXIE_DATA_DIR=/data`**, **`PORT`** for uvicorn), [`.dockerignore`](../../lexie-server/.dockerignore), and [README — Deploy (M1 / WX-006)](../../lexie-server/README.md#deploy-m1--wx-006). Registry: **WX-006** `in_progress`. **Remaining:** create app on chosen host, secrets, volume/DB, **`https://…/health`** (**D2**).

2026-05-04 — **WX-019** / **WX-020** — PM: **A3 = Fly.io**, capacity note, Fly metrics API + **`fly-metrics.net`** pointers in [`work-inventory.md`](work-inventory.md). Code: **`explain_telemetry`** table + **`LEXIE_STORE_TELEMETRY`**, **`ExplainPipelineResult`** / per-stage timings in pipeline, explain router inserts. Registry: **WX-019** `done`, **WX-020** `done`.

2026-05-04 — **WX-021** — Admin **`GET /admin/telemetry/summary`**, **`/recent`**, **`/count`**; **`/admin`** HTML telemetry panel; [README — Observability (WX-021)](../../lexie-server/README.md#observability-wx-021) (Fly Prometheus + **`fly-metrics.net`** runbook). **`pytest`:** **22** passed. Registry: **WX-021** `done`.

2026-05-04 — **WX-007** — Started M2 Part E: registry **`in_progress`**; [README — Child browser prototype (M2 / WX-007)](../../lexie-server/README.md#child-browser-prototype-m2--wx-007); **`/prototype/`** JSON error detail + **`X-Explain-Latency-Ms`** in UI hint.

2026-05-04 — **WX-007** → **`done`** (prototype + Fly); **WX-008** → **`in_progress`** — [README — Parent admin on production (M3 / WX-008)](../../lexie-server/README.md#parent-admin-on-production-m3--wx-008); Part **F1–F4** verification on **`lexie-server.fly.dev/admin`**.

2026-05-04 — **WX-008** → **`done`** — **F1–F4** verified on prod **`/admin`** + profile-driven explains. **WX-009** → **`in_progress`** (M4 Part G manual E2E per validation matrix).

2026-05-04 — **WX-006** — **`fly launch`** completed; app live at **`https://lexie-server.fly.dev`**, **`GET /health`** → **200** (**D2**). Registry: **WX-006** `done`. **Follow-up:** **`fly secrets set`** for production keys + **`LEXIE_CORS_ORIGINS`**; **D3**/**D5** manual.

2026-05-06 — **Regression / architecture plan execution** — [`.env.example`](../../lexie-server/.env.example): **`LEXIE_CHAT_MODEL`**, **`LEXIE_TTS_MODEL`**, **`LEXIE_TTS_VOICE`** align with [`config.py`](../../lexie-server/lexie_server/config.py). **`main.py`:** drop redundant **`init_engine`** after **`create_tables`**. **`explain.py`:** cached OpenAI client by API key; fix **`Response(status_code=…)`** on ValueError/502/500 branches (was passing status as positional `content`). **Tests:** **`test_config.py`**, expanded **`test_api.py`**, **`test_telemetry.py`** (**413** + telemetry). **`pytest`:** **32** passed.

2026-05-05 — **WX-009** → **`done`** — M4 Part **G** taken as a win for this pass: **`/prototype/`** **G1** on prod; root cause of ~**11 s** server latency traced to **`LEXIE_HEADWORD_TTS=1`** (second TTS + MP3 concat ~**2.7 s** in instrumented run); **`LEXIE_HEADWORD_TTS=0`** in [`.env.example`](../../lexie-server/.env.example), **`fly secrets set`**, **`fly deploy`**; prod warm **`X-Explain-Latency-Ms`** ~**6227**. Git **`41448e1`** (fly.toml + example). **G2–G7** matrix rows left for **ad hoc**. Registry: **WX-009** `done`; **active:** **WX-010**.

2026-05-06 — **WX-010** → **`in_progress`** — M5 **Part H** ([`work-inventory.md` § Part H](work-inventory.md#part-h-m5--steady-operation-wx-010)): **H1** **`LEXIE_LOG_REQUESTS=0`** on Fly after any debug; **H2** OpenAI billing alerts + rough **$/explain** after a week of use; **H3** Fly **`min_machines_running`** / cold first explain vs [RUNBOOK §4](../../lexie-docs/lexie/committed-to-build/lexie-word-explainer.RUNBOOK.md); **H4** re-read [journeys-and-observability §3](../../lexie-docs/lexie/committed-to-build/lexie-word-explainer.journeys-and-observability.md). Registry narrative + **WX-010** row + inventory **Last updated**.

2026-05-06 — **WX-010 / H1** — **`fly secrets list`** on **`lexie-server`**: no **`LEXIE_LOG_REQUESTS`** (and no **`LEXIE_STORE_TELEMETRY`**) secret → app uses code defaults **`False`** / **0** ([`config.py`](../../lexie-server/lexie_server/config.py)). **H1 prod OK** unless you previously set a secret (then remove or set **`LEXIE_LOG_REQUESTS=0`**). **H3** confirmed in repo [`fly.toml`](../../lexie-server/fly.toml): **`auto_stop_machines = 'off'`**, **`min_machines_running = 1`**, **`/health`** check.

2026-05-07 — **WX-010** → **`done`** — M5 Part **H** closed: **H1** Fly defaults; **H2** OpenAI org budget **$50/mo**, **80%/100%** email alerts, **$120** usage limit (user confirmed Limits UI); **H3** Fly cold-start posture in **`fly.toml`**; **H4** [journeys-and-observability §3](../../lexie-docs/lexie/committed-to-build/lexie-word-explainer.journeys-and-observability.md) accepted for Phase 1. **D3** cellular **`/health`** + **D5** **`BASE_URL`** in 1Password remain optional. Registry + inventory: **active → WX-012** (§11 sign-off).

2026-05-01 — **WX-012** → **`done`** — Part J [SPEC §11](../../lexie-docs/lexie/committed-to-build/lexie-word-explainer.SPEC.md) definition-of-done for **`https://lexie-server.fly.dev`**: **`GET /health`** / **`/admin`** / **`/prototype/`** **200**; **`POST /explain`** no device key **401**; Fly defaults align with **§11** item 5; **`pytest`** **32** passed ([`lexie-server`](../../lexie-server)); **lexie-ops** Vite proxy to Fly — **`GET /__lexie/health`** **200** (`monitoring/lexie-ops`, `VITE_PROXY_TARGET`); prototype [`index.html`](../../lexie-server/lexie_server/static_prototype/index.html) — **§5.1** child-facing error strings (incl. FastAPI **`detail.error`**). **§8.2 p95 &lt;5 s:** **exception** — observed warm **`X-Explain-Latency-Ms`** ~**6–7 s** with **`LEXIE_HEADWORD_TTS=0`** (OpenAI + host); noted in SPEC §11. **Next:** optional **WX-011** only.

2026-05-03 — **WX-011** → **`done`** — Part I [lexie-ops](../../monitoring/lexie-ops): **I1–I4** per [`work-inventory.md` § Part I](work-inventory.md#part-i--lexie-ops-optional-wx-011). README **Point at production** + [`.env.example`](../../monitoring/lexie-ops/.env.example) Fly proxy line. **`VITE_PROXY_TARGET=https://lexie-server.fly.dev`**, Vite **`127.0.0.1:5175`**, **`GET /__lexie/health`** → **200** JSON. **Next:** ad hoc **WX-006** **D3**/**D5** only.

2026-05-03 — **WX-022** → **`done`** — Connectivity de-risk complete. **D3:** phone Wi-Fi off, cellular on → `GET https://lexie-server.fly.dev/health` → **200** `{"ok":true}` ✓. **Hotspot stretch:** phone hotspot on; laptop joined hotspot Wi-Fi → `/health` → **200** ✓. **D5:** `BASE_URL` `https://lexie-server.fly.dev` saved in 1Password (Secure Note: "Lexie — production URL"). Closes **WX-006** D3/D5 tail. Server reachable from both cellular and hotspot network paths — LX-4 device connectivity de-risked. Registry: **WX-022** `done`.

2026-05-03 — **WX-023** — Firmware handoff doc written: [`lexie-word-explainer.DEVICE-INTEGRATION.md`](../../lexie-docs/lexie/committed-to-build/lexie-word-explainer.DEVICE-INTEGRATION.md) — normative contract for LX-4: `BASE_URL`, `GET /health`, `POST /explain` shape + auth, limits (2 MiB / 30 s / 0.4 s), TLS requirements, `LEXIE_DEVICE_KEY` flash-at-provisioning, what is NOT for device (`/profile`, `/admin`, `/admin/telemetry/*`), SPEC §5.1 error codes with device actions, OpenAI architecture note. Linked from [`phased-delivery-plan.md`](../../lexie-docs/lexie/committed-to-build/phased-delivery-plan.md) and [`lexie-docs/REGISTRY.md`](../../lexie-docs/REGISTRY.md). Registry: **WX-023** `backlog → done (doc exists)`.

2026-05-03 — **WX-025** → **`done`** — WiFi provisioning design locked: **USB serial, one-time setup** (USB-C cable = provisioning + charging cable). Config JSON written to XIAO flash: **3 network profiles** (home AP + 2 parent hotspots) + `BASE_URL` + `LEXIE_DEVICE_KEY`. Firmware tries SSIDs in order on boot; auto-switches between home / hotspot. SoftAP noted as Phase 3 upgrade. Registry: **WX-025** `done`. **All Phase 2 bridge items complete — hardware development can begin.**

2026-05-03 — **WX-024** → **`done`** — LX-4 platform locked: **Seeed XIAO ESP32-S3** (~$8, 21×17.5 mm, USB-C LiPo charging, I2S native, MicroPython). Audio: **INMP441** I2S MEMS mic + **MAX98357A** I2S amp + slim 8Ω 0.5W speaker. Battery: **503040** thin LiPo ~400 mAh. Transport: WAV/PCM to start. Pi Zero 2W rejected on boot time (~30 s is a product-level problem for an instant-use Lexie Card). Registry: **WX-024** `done`. Next: **WX-025** WiFi provisioning design.

2026-05-03 — **PM restructure** — Phase 1 complete; Phase 2 (LX-4) pivot captured in PM. [`work-inventory.md`](work-inventory.md): Section B reframed as "Phase 1 complete — Phase 2 starting"; Section D (Phase 2 hardware track: **WX-022–025**) and Section E (parallel software polish, non-blocking) added; quick index updated. [`registry.md`](registry.md): top narrative updated to Phase 2 active; **WX-022–025** rows added. [`phased-delivery-plan.md`](../../lexie-docs/lexie/committed-to-build/phased-delivery-plan.md): Phase 2 row marked **active / in progress** with pointer to DEVICE-INTEGRATION.md. [`lexie-docs/REGISTRY.md`](../../lexie-docs/REGISTRY.md): **LX-1** → "Phase 1 shipped (2026-05-03)"; **LX-4** → "Phase 2 / active".

2026-05-03 — **Lexie Card** locked as **final LX-4 v1 form factor** (ID-1 footprint, ≤ ~8 mm, beside-the-book placement, **no magnets** / clip in v1). Canonical spec: [`hardware/lexie-plaud-form-factor.html`](../../hardware/lexie-plaud-form-factor.html). PRD, SPEC connectivity wording, DEVICE-INTEGRATION, phased plan, registries, `hardware/README.md`, shopping cart, and supersession banners on earlier bookmark/clip HTML aligned to this story.

2026-05-05 — **WX-026–WX-033** — LX-4 **bench bring-up** work items opened in [`registry.md`](../registry.md) (**backlog**); narratives in [`work-inventory.md`](../work-inventory.md) **Section D2** (unbox → solder/polarity → MicroPython → pin map → I2S out/in → USB **`config.json`** provisioning → TLS **`/health`** + **`/explain`**). Assumes BOM parts on hand. **Next:** execute **WX-026** onward.

2026-05-05 — **WX-026** → **`done`** — Unbox/inventory captured in [`items/WX-026.md`](../items/WX-026.md) (M1 bench BOM table + XIAO non-Sense verification). **Next:** **WX-027** solder + LiPo polarity before first JST.

2026-05-22 — **WX-034** → **`done`** — **Platform pivot Path A → Path B (Waveshare).** Decision: [lx-4-platform-pivot-waveshare.md](../lx-4-platform-pivot-waveshare.md). **PRD set:** [lx-4-waveshare-device.PRD.md](../../lexie-docs/lexie/prds/lx-4-waveshare-device.PRD.md), [lx-4-device-firmware.PRD.md](../../lexie-docs/lexie/prds/lx-4-device-firmware.PRD.md), [lx-4-device-ux-sla.PRD.md](../../lexie-docs/lexie/prds/lx-4-device-ux-sla.PRD.md). **WX-027** **superseded**; Path A parts = spare.

2026-05-22 — **WX-035** → **`in_progress`** — **Waveshare ESP32-S3-AUDIO-Board delivered.** LX-4 PRDs bumped to **v1.1** (execution). PINMAP draft: [lexie-word-explainer.PINMAP-WAVESHARE-AUDIO.md](../../lexie-docs/lexie/committed-to-build/lexie-word-explainer.PINMAP-WAVESHARE-AUDIO.md). **Next:** USB-C bench power → **full flash erase** → do **not** run stock demo on home Wi‑Fi. Then **WX-028**.

2026-05-22 — **WX-035 erase** — `python3 -m esptool --chip esp32s3 -p /dev/cu.usbmodem101 erase_flash` → **Chip erase completed successfully** (6.9s). MAC `3c:dc:75:6e:a4:d4`. Vendor firmware removed. **Next:** **WX-028** (flash smoke image). Port may change on replug (`usbmodem101` vs `1101`).

2026-05-22 — **WX-028** — USB reconnect + MicroPython v1.25.0 SPIRAM_OCT reflash OK. REPL: **lexie_repl_ok micropython v1.25.0**. Wi-Fi scan: **25** APs visible. **Remaining:** join one AP (`./tools/wx028-wifi-smoke.sh 'SSID' 'pass'`) to close WX-028 fully. **Next milestone:** **WX-036** codec.

2026-05-22 — **WX-036 Phase 1** → **pass** — ESP-IDF codec demo: KEY beeps + BOOT WAV on speaker; ES8311/I2S init OK. **WX-029** pin map verified from boot log. Re-flashed MicroPython for Path A.

2026-05-22 — **WX-029** → **`done`** — [PINMAP-WAVESHARE-AUDIO.md](../../lexie-docs/lexie/committed-to-build/lexie-word-explainer.PINMAP-WAVESHARE-AUDIO.md) checklist ticked (WX-036 evidence).

2026-05-22 — **WX-032** → **`in_progress`** — Reliability harness: `lexie_e2e` stress mode (10× canned WAV), `wx032-stress.py`, `wx032-reliability.sh`. Playback hardening (callback wait, retries). **Next:** close monitor, run `./tools/wx032-reliability.sh /dev/cu.usbmodem101` for 10/10 gate.

2026-06-09 — **WX-032 done** — Manual PTT smoke passed (stress off, PTT mode). Combined with 10× automated gate (`pass=10 fail=0`), Path B Lexie E2E bench complete.
